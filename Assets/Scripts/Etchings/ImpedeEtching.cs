using UnityEngine;
/*
namespace Etchings
{
    public class ImpedeEtching : BlockEtching
    {
        protected override bool TestActivation()
        {
            if (UsesUsedThisTurn >= usesPerTurn) return false;
            
            var enemy = ActiveEnemiesManager.Current.CurrentEnemy;

            if (enemy.StickNum != Stick.GetStickNumber()) return false;
            
            Stick.SetTempColour(color);
            enemy.Block();
            UsesUsedThisTurn += 1;
            return true;

        }
    }
}
*/
